version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 16
    commands:
      - echo Installing...
      - npm install
  build:
    commands:
      - echo Build complete
artifacts:
  files:
    - '**/*'

